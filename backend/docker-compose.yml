services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: sofia_villa
      POSTGRES_USER: sofia
      POSTGRES_PASSWORD: sofia_dev_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
